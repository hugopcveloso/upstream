import { createBrowserHistory } from 'history'; 

export default createBrowserHistory();

// We use this for programmatic navigation in the action creator.
// to redirect in the components we can use a different way. (google it)